# 延后接入的模型清单

以下模型只保留 ID、任务绑定和输入输出语义，当前均未携带权重或专属运行环境：

| 任务族 | 稳定模型 ID 示例 | 当前状态 |
| --- | --- | --- |
| LayerPulse 基础模型 | `layerpulse_geochronograph_f3x200cf` | `adapter_required` |
| 断层 | `faultseg_3d`, `faultnet_china_field` | `adapter_required` |
| 地层/层位 | `seismic_surface_seg`, `wellfuse_horizon_p17`（发布合同：`wellfuse_p17_horizon`） | `adapter_required` |
| 井震注册 | `wellfuse_align_p13`, `WellFuse-GeoAlign-12B-Direct-v1`, `wellfuse_align_geopath_tie_v1` | `adapter_required` |
| 地震相/地质体 | `wellfuse_facies_*`, `wellfuse_channel_p17`, `wellfuse_karst_p17` | `adapter_required` |
| 井侧物性/流体/裂缝 | `wellfuse_*_p18`, `wellfuse_*_northwest_fast`, `wellfuse_fluid_interpretation_fast`, `wellfuse_fracture_development_utah_fast` | `adapter_required` |

科学状态/历史证据仍可通过 release 合同读取，但不等于本目录具备可运行权重。失败或
已归档记录继续保持 `blocked`/只读语义，不会被接口模式重新启用。
