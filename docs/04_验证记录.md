# 抽离后验证记录（2026-09-03）

- 前端原样比对：源/目标各 50 个文件，`missing=0`、`extra=0`、`changed=0`；
- 静态边界检查：`python tools/verify_platform_skeleton.py` 通过；
- 接口模式检查：在包含完整 Web 依赖的 Python 3.11 运行时执行
  `python tools/verify_platform_skeleton.py --runtime` 通过；仅有 FastAPI 而没有
  `uvicorn` 的系统 Python 会按预期被拒绝；
  `/api/v1/health`、`/api/v1/capabilities`、根页面均可访问，模型 35 个、预测运行器 0 个、
  预测任务 7 个且均无 `runnable_model_ids`；
- 启动器探针会同时检查 `fastapi、uvicorn、numpy、PyYAML、pyproj` 和 API 导入；
  当前机器的旧本地运行时缺 `pyproj`、系统 Python 缺 `uvicorn` 时会明确列出缺项，
  不再误报“已启动”；使用完整 Python 3.11 运行时实测可正常启动并由停止脚本回收；
- 任务门控检查：提交 `faultseg_3d` 预测请求返回结构化 HTTP 422，不创建虚假任务；
- 前端源工作树回归：Node 测试 97/97 通过。抽离目录不含 `node_modules`，目标机需先按
  `frontend/package-lock.json` 安装依赖后再运行测试。
